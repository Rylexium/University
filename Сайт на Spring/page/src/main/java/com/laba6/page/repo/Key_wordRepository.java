package com.laba6.page.repo;

import com.laba6.page.models.Key_word;
import org.springframework.data.jpa.repository.JpaRepository;

public interface Key_wordRepository extends JpaRepository<Key_word, Integer> {
}
